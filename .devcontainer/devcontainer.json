{
	"name": ".net core koans",
	"extensions": [
		"vsls-contrib.codetour",
		"ms-dotnettools.csharp"
	],
	"settings": {
		"terminal.integrated.shell.linux": "/bin/bash",
		"files.exclude": {
			"**": true, //the dockerfile will load all the file
		}
	},
	//"postCreateCommand": "dotnet restore",
	"build": {
		"dockerfile": "../Dockerfile",
		"args": {
			"VARIANT": "3.1"
		}
	}
}